# 狀態管理系統更新說明

## ✅ 完成的修改

### 1. 狀態邏輯調整

**舊邏輯：**
- `favorite` - 收藏
- `unreviewed` - 未審核（儲存在 JSON 中）
- `reviewed` - 已審核（預設，不儲存）

**新邏輯：**
- `favorite` - 收藏（儲存在 JSON 中）
- `reviewed` - 已審核（儲存在 JSON 中）
- `unreviewed` - 未審核（**虛擬狀態**，表示不在 favorite 也不在 reviewed 列表中）

### 2. JSON 結構變化

**舊結構：**
```json
{
  "manga": {
    "favorite": [],
    "unreviewed": []
  }
}
```

**新結構：**
```json
{
  "manga": {
    "favorite": [],
    "reviewed": []
  }
}
```

### 3. 篩選邏輯

**全部：** 顯示所有項目

**收藏：** 顯示在 `favorite` 列表中的項目

**未審核：** 顯示**不在** `favorite` 也**不在** `reviewed` 列表中的項目（反向邏輯）

### 4. 修改的文件

- ✅ `src/core/status_manager.py` - 完全重寫，實現新邏輯
- ✅ `src/app.py` - 更新初始化邏輯，自動遷移舊數據
- ✅ `data/status.json` - 自動更新為新結構

### 5. StatusManager 關鍵方法

#### `get_status(category, item_path)`
返回項目的當前狀態：
- 如果在 `favorite` 列表中 → 返回 "favorite"
- 如果在 `reviewed` 列表中 → 返回 "reviewed"  
- 都不在 → 返回 "unreviewed"（虛擬狀態）

#### `set_status(category, item_path, status)`
設置項目狀態：
- 設為 "favorite" → 加入 `favorite` 列表，從其他列表移除
- 設為 "reviewed" → 加入 `reviewed` 列表，從其他列表移除
- 設為 "unreviewed" → 從所有列表中移除

#### `filter_by_status(category, items, status)`
根據狀態篩選項目列表：
- "favorite" → 返回在 `favorite` 列表中的項目
- "reviewed" → 返回在 `reviewed` 列表中的項目
- **"unreviewed" → 返回不在任何列表中的項目（反向邏輯）**

### 6. 自動數據遷移

啟動服務器時會自動：
1. 檢查 `status.json` 是否存在
2. 如果存在舊的 `unreviewed` 鍵，則刪除
3. 如果缺少 `reviewed` 鍵，則添加空數組
4. 保存更新後的結構

## 🎯 使用示例

### 收藏流程
1. 用戶在 Reader 點擊星星
2. 調用 `set_status('manga', '海賊王', 'favorite')`
3. '海賊王' 被加入 `manga.favorite` 列表
4. 如果之前在 `reviewed` 列表中，會被移除

### 標記為已審核
1. 用戶取消收藏
2. 調用 `set_status('manga', '海賊王', 'reviewed')`
3. '海賊王' 從 `favorite` 移除，加入 `reviewed` 列表

### 未審核篩選
1. 用戶點擊「未審核」按鈕
2. API 調用 `filter_by_status('manga', all_mangas, 'unreviewed')`
3. 返回所有不在 `favorite` 也不在 `reviewed` 中的漫畫

## 📊 狀態轉換圖

```
新漫畫（未審核）
    ↓ 點擊收藏星星
收藏（favorite）
    ↓ 再次點擊星星
已審核（reviewed）
    ↓ （無法直接變回未審核）
```

**注意：** 一旦標記為 `reviewed` 或 `favorite`，項目就不會再出現在「未審核」篩選中。

## ✅ 測試結果

```
✅ StatusManager 初始化成功
✅ JSON 結構正確
✅ 狀態讀取正常
✅ 狀態設置正常
✅ 篩選邏輯正常（包括反向的 unreviewed）
✅ 服務器啟動成功
```

## 🔜 下一步

前端還需要更新（但後端已完全就緒）：
- 測試 Reader 頁面收藏功能
- 測試列表頁面篩選功能
- 確認「未審核」篩選顯示正確的項目

