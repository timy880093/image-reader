# 狀態檔案路徑統一說明

## 問題描述

之前系統中存在兩個 `status.json` 檔案:
- `data/status.json` (專案根目錄)
- `src/data/status.json` (實際使用的檔案)

這導致用戶查看根目錄的 status.json 時看不到收藏記錄,造成混淆。

## 解決方案

### 1. 新增配置參數

在 `config.toml` 中新增狀態檔案路徑配置:

```toml
[manga]
# 狀態檔案設定
status_file_path = "./data/status.json"  # 狀態檔案路徑（收藏、已審核等狀態）
```

### 2. 修改 app.py

修改 `src/app.py`,讓 StatusManager 使用配置檔案中指定的路徑:

```python
# 初始化狀態管理器 - 使用配置檔案指定的路徑
status_file_path = config['manga'].get('status_file_path', './data/status.json')
# 如果路徑是相對路徑,從專案根目錄計算
if not Path(status_file_path).is_absolute():
    project_root = Path(__file__).parent.parent
    status_file_path = str(project_root / status_file_path)

print(f"📊 狀態檔案路徑: {status_file_path}")
status_manager = StatusManager(status_file_path)
```

### 3. 路徑解析邏輯

- 相對路徑 (`./data/status.json`): 從專案根目錄 (`e:\_code\vibe\manga-reader`) 開始計算
- 絕對路徑 (`E:/custom/path/status.json`): 直接使用該路徑

### 4. 資料遷移

已將 `src/data/status.json` 的收藏記錄合併到 `data/status.json`:

```json
{
  "manga": {
    "favorite": ["第一大弟子", "下一颤，性福"],
    "reviewed": []
  },
  "gallery": {
    "favorite": ["5238-タカハル"],
    "reviewed": []
  }
}
```

## 優點

1. ✅ **統一管理**: 所有狀態檔案路徑由配置檔案統一管理
2. ✅ **靈活配置**: 用戶可以自訂狀態檔案存放位置
3. ✅ **清晰可見**: 啟動時會在 console 顯示實際使用的路徑
4. ✅ **避免混淆**: 不再有多個 status.json 檔案

## 使用方式

### 查看狀態檔案位置

啟動應用時,會在 console 顯示:
```
📊 狀態檔案路徑: E:\_code\vibe\manga-reader\data\status.json
```

### 自訂狀態檔案位置

編輯 `config.toml`:

```toml
[manga]
# 使用相對路徑（從專案根目錄開始）
status_file_path = "./custom_data/status.json"

# 或使用絕對路徑
status_file_path = "E:/my_data/manga_status.json"
```

## 注意事項

- 舊的 `src/data/status.json` 檔案可以保留作為備份,或手動刪除
- 修改路徑後需要重啟應用才會生效
- 確保指定的目錄有寫入權限

## 更新時間

2026-01-11
